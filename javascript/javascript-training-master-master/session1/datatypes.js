//js primitives
var userName = "priya"; //strings
var age = 12;// number
var bool = true ;//boolean
var nothing = null;
var undef = undefined;

//undefined is the default value for all the variables in JS.
//null is a specific value that must be given to a variable.


//Objects in JS are just key value pairs
var person = {
    name : "Nila",
    age : 24,
    profession: 'Trainee'
}

console.log(userName);
window.alert(userName);